# Runs — pull-krisp evals

> Журнал прогонов benchmark. Одна строка = один сценарий в одном прогоне. Сценарии и критерии — в `EVAL.md`. В заметке указывать: симуляция (mock) или реальный прогон.

| Дата | Модель | Кейс | Pass/Fail | Заметка |
|------|--------|------|-----------|---------|
